extends layout.pug 
block content

    a.btn(href='/countriesrep') Reverse Order

    table
      tr
        th Name 
        th Population
      
      each country in countries
        tr 
          td #{country.name}
          td #{country.population}